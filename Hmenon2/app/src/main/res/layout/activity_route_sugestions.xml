<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/btnOkRoute"
        android:id="@+id/listRouteSugestion"></ListView>

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imageButtonPrevious"
        android:layout_alignParentBottom="true"
        android:src="@drawable/prev_button_icon"/>

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imageButtonNext"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:src="@drawable/next_button_icon"/>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="65dp"
        android:id="@+id/btnOkRoute"
        android:layout_alignBottom="@+id/imageButtonPrevious"
        android:layout_centerHorizontal="true"
        android:text="OK"/>
</RelativeLayout>